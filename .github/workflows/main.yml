name: iOS CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted
    
    steps:
        - name: check Xcode version
          run: /usr/bin/xcodebuild -version

        - name: checkout repository
          uses: actions/checkout@v3
    
        # - name: Install dependencies
        #   run: |brew install swiftlint 

        - name: Build and test
          run: |
             - xcodebuild -project ALM\ APP\ Dem0.xcodeproj -scheme ALM\ APP\ Dem0 -configuration Debug 
